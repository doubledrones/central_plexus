[alias]
	st = "status -sb"
	ci = commit
	co = checkout
	br = branch
	di = diff
	dih = diff HEAD
	diw = "diff --word-diff"
	upstream = !"git fetch origin -v; git fetch upstream -v; git merge upstream/master"
[color]
	branch = auto
	diff = auto
	interactive = auto
	status = auto
