#!/bin/sh

for FEATURE in `ls -dr features/*.feature`
do
  case $@ in
    *$FEATURE*)
      echo "Ommiting feature: $FEATURE"
      ;;
    *)
      echo
      echo $FEATURE
      echo
      cucumber $FEATURE || exit $?
      echo
      ;;
  esac
done
