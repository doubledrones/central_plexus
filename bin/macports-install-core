#!/bin/sh -e

PORTS="
git-core
htop
p5-app-ack
pbzip2
lzmautils
watch
wget
macvim
tmux
"

# TODO replate to for
echo "$PORTS" | while read line
do
  if [ -n "$line" ]
  then
    port install $line
  fi
done
